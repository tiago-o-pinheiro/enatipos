# Public origin used as metadataBase for OG / Twitter / canonical URLs.
# In Vercel set this per-environment (Production / Preview / Development).
# Falls back to http://localhost:3050 in dev if unset.
NEXT_PUBLIC_SITE_URL=https://your-domain.com
